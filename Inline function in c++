/* c++ program to find cube of a 
number using inline function */

#include <iostream>
using namespace std;

inline int cube(int a)
{
    return (a*a*a);
}

int main()
{
    int a;
    cout<<"enter the number to find cube: ";
    cin>>a;
    cout<<"cube of "<<a<<" is:"<<cube(a);
    return 0;
}
